apiVersion: v1
kind: Service
metadata:
  name: promotion
spec:
  selector:
    app: promotion
  type: ClusterIP
  internalTrafficPolicy: Local
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
